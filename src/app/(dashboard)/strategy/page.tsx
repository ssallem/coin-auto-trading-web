/**
 * 전략 설정 페이지 (Server Component shell)
 *
 * StrategyContent 클라이언트 컴포넌트를 렌더링합니다.
 */
import { StrategyContent } from '@/components/strategy/strategy-content'

export default function StrategyPage() {
  return <StrategyContent />
}
