/**
 * 거래 페이지 (Server Component shell)
 *
 * TradeContent 클라이언트 컴포넌트를 렌더링합니다.
 */
import { TradeContent } from '@/components/trade/trade-content'

export default function TradePage() {
  return <TradeContent />
}
