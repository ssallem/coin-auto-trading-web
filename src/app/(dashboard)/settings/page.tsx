/**
 * 봇 설정 페이지 (Server Component shell)
 *
 * SettingsContent 클라이언트 컴포넌트를 렌더링합니다.
 */
import { SettingsContent } from '@/components/settings/settings-content'

export default function SettingsPage() {
  return <SettingsContent />
}
